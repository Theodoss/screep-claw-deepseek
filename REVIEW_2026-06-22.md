# Code Review — new-code（2026-06-22）

> Review by Claude（不做主要修改，輸出問題清單給 deepseek）。
> **重要前提**：sandbox 無法 SSH `git pull`（host key 驗證失敗）。本 review 基於本地 HEAD `8697591 deploy 20260621-141736`。
> **請先在本機 `git pull` 確認本地是最新**，若 deepseek 已推新 commit，本清單部分項目可能已修。

---

## 0. 結論摘要

最大問題不是新 bug，而是 **DECISIONS.md D007 / D008 與 `INSTRUCTION_expansion_W47N22.md` 規劃的修改幾乎全部尚未落地**。code 仍是規劃前的舊狀態。下面 A 段是「文檔說要做、但 code 沒做」，B/C 段是獨立於擴張任務的架構與健壯性問題。

優先順序：**A1 → A2 → A3 →（再考慮 B/C）**。

---

## A. 既定決策未落實（最高優先）

### A1. travel.js 沼澤邊界橫跳修正未實作（D007 任務 0）
`new-code/travel.js` 仍使用舊策略：每 tick `findExit` + `findClosestByPath(exitDir)` → `moveTo(exit)`（第 116–139 行）。
- D007 診斷的根因（沼澤 fall back → 推回邊界 → routeIdx 失配 → `ERR_NO_PATH` → 重建 → 無限橫跳）**完全未處理**。
- 缺：① 改為對 `nextRoom` 中心 `moveTo`（相鄰房、單邊界）；② 邊界 tile 防呆（x/y=0|49 先往內走一格）；③ 反橫跳鎖定（`_t.lastRoom` + `lockUntil`）。
- 影響：claimer / pioneer / reserver 任何經過 W48N22 沼澤房都會卡死。**擴張任務的前置硬條件。**
- 指示：照 `INSTRUCTION_expansion_W47N22.md` 任務 0 三步實作。

### A2. 擴張任務狀態機完全未實作（D008 任務 a–f）
全 `new-code` grep `expansionMission` = **0 筆**。目前擴張走的是 `manager.remote.js` 的舊 `EXPANSION_TARGETS.W47N22` 區塊（第 14–22、944–1005 行），與 D008 規格不符：

| 項目 | 現況（舊 code） | D008 規格 | 問題 |
|---|---|---|---|
| claimer body | `[CLAIM×3, MOVE×3]` = 1950 | `[CLAIM×2, MOVE×10]` = 1700 | 現況沼澤 30 fatigue/步、僅 6 回復 → 幾乎不動，正中 A1 橫跳 |
| pioneer body | `[6W,6C,12M]` = 1500 | `[5W,4C,5M]` = 950 | 現況空車過沼澤負重高、慢 |
| pioneer 數量 | 只生 1（`countCreepsInRole==0`，第 982 行）| `builderCount`(=3) | 建 spawn（15,000 能量）嚴重不足 |
| GCL 守門 | 無 | GCL < 已擁有房+1 不生 claimer | 現況 GCL 不足會白生 1950 能量 3-CLAIM creep 去 reserve（壽命僅 600，純浪費）|
| 停母房 upgrade | 無 | spawn 蓋好前停 upgrade，保 controllerEmergency 後援 | `role.upgrader` / `role.rcl1Upgrader` / `manager.rcl2ContainerEconomy` 皆無 `expansionMission` guard（已驗證 0 筆）|
| 狀態機 | 無 | `Memory.expansionMission` phase: claim→build→done | 無收尾，done 後不會自動恢復母房 upgrade |

- 指示：照 `INSTRUCTION_expansion_W47N22.md` 任務 1–4 實作；舊 `EXPANSION_TARGETS` 區塊與新狀態機**擇一**（建議用 mission 取代 EXPANSION_TARGETS，避免兩套擴張邏輯並存衝突）。

### A3. 清理未執行（D007 次要）
- `new-code/nav.flag.js`：死碼，無任何檔案 require（已驗證），且語義與 travel.js 衝突 → 刪除。
- `new-code/manager.remote.js.bak`（31KB）：仍被 git 追蹤（`git ls-files` 確認）→ `git rm --cached`，並把 `*.bak` 加進 `.gitignore`。

---

## B. 架構性問題（獨立於擴張任務，建議排程處理）

### B1. 單一 home room 硬編碼 `'W49N25'`
散落於 `main.js`（含 defense spawn 區塊 121–135 行）、`manager.remote.js`（`HOME_ROOM` 常數，`run()` 只跑這一房）、`manager.remoteDefense.js`。
- 後果：**擴張成功後 W47N22 不會被 remote / remoteDefense 系統涵蓋**；本質上無法支援第二殖民地。
- 與「長征 + 擴張」的專案目標直接衝突。建議：把 home 改成「掃描所有 `controller.my` 的房」或一份 colony 設定表。

### B2. 單一 spawn 假設 `spawns[0]` 遍布全 code
`main.js:122`、`manager.rcl2ContainerEconomy`、`manager.remote`、`manager.spawn`、`manager.rcl1Bootstrap`、`role.guard`、`role.remoteHauler` 等皆只取 `spawns[0]`。
- RCL7 = 2 spawn、RCL8 = 3 spawn，目前只用第一個 → **spawn 吞吐瓶頸**，正好卡在「RCL6 長征」要衝產量的階段。
- 建議：抽一個 `getAvailableSpawn(room)`（回傳第一個 `!spawning` 的 spawn），並讓各 manager 共用 spawn 佇列，避免互搶。

### B3. 兩套擴張機制並存
`EXPANSION_TARGETS`（remote 內）與規劃中的 `Memory.expansionMission` 語義重疊。落地 D008 時務必統一成一套，否則 claimer 會被兩邊各自排程。

---

## C. 正確性 / 健壯性（次要）

- **C1**　`manager.remote.js` claimer GCL fallback：`role.claimer.js` 第 40–46 行在 `ERR_GCL_NOT_ENOUGH` 時改 reserve。搭配「無 GCL 守門就 spawn」（A2），會出現 1950 能量、3-CLAIM、壽命 600 的 creep 只去 reserve，性價比極差。修 A2 守門即可解。
- **C2**　`travel.js` 抵達判定：在 targetRoom 但離邊界 < 3 時不清 `_t`，靠 routeIdx 收尾（第 72–112 行）。可運作但脆弱，建議與 A1 一起重寫。
- **C3**　`pioneer` 無工地時 fallback 去 harvest + upgrade（第 29–45 行）。擴張情境下 spawn 工地由玩家手動放，工地出現前 pioneer 會先 upgrade 目標房 controller，與「資源集中建 spawn」略有出入；非阻斷性，可接受。

---

## D. 可維護性 / CPU（低優先）

- **D1**　`manager.population.js` 第 431–432 行 upgrader target/limit 用多層巢狀三元，極難維護且 target/limit 邊界易出錯。建議抽成查表函式 `upgraderCountFromWork(work)`。
- **D2**　`manager.economy.js` `getStoredEnergy` 每 tick 對每房 `FIND_STRUCTURES` 全掃 + `FIND_MY_CREEPS`；`update()` 內多處重複 find。單房可接受，擴張後 CPU 線性上升。建議快取或降頻。

---

## 建議執行順序（給 deepseek）

1. **A1**（travel.js 沼澤修正）— 擴張前置，先做。
2. **A2**（expansionMission 狀態機 + body + GCL 守門 + 停母房 upgrade）。
3. **A3**（刪 nav.flag.js、移除 .bak 版控）。
4. 完成後把改動補寫進 `DECISIONS.md`（引用 D007 / D008），更新 `project_state.md`。
5. B1 / B2 列為「長征後、擴張穩定前」的架構重構項，單獨排程。

> 所有改動只動 `new-code`；改完務必更新改變文檔讓 Claude / deepseek 同步。
