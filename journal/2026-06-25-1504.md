# Screeps Journal 2026-06-25 15:04 EDT

## 觀察
- W48N25 有 invader core (level 0, detected in remoteDefense memory)
- 有一隻 guard_core_4825_71916033 剛生出來（tick 71916033），但舊架構的 body 太貴：
  - RCL6 melee guard: T×6 A×8 H×1 M×15 = **1700e**
  - 等能量累積太久才 spawn，且 TOUGH/HEAL 對打 core 沒用（core 傷害光環很弱）
- user 指示：做純粹的 core cleaner，只要 ATTACK+MOVE

## 改動
1. **新檔案**: `role.coreCleaner.js` — 純 core 破壞者
   - 去 core room → 找 INVADER_CORE → attack
   - 不再有 core 時返回母房
   - 無 THOUGH/HEAL/RANGED，就是 A+M 硬砸

2. **修改 `manager.remoteDefense.js`**:
   - 新增 `buildCoreCleanerBody()`: ATTACK+MOVE pairs，吃到 energyAvailable 預算
     - 每對 130e (80+50)，RCL6 full energy ~17 pairs = 34 parts, ~2210e
     - 但會隨實際 energyAvailable 自動縮小，energy >=260e 就能生
   - `getCoreSpawnRequests` 改用 coreCleaner 角色取代 guard/remoteGuard pair
   - 每個 core 只生 1 隻 cleaner

3. **修改 `main.js`**:
   - 加入 coreCleaner require + ROLE_MODULES
   - 加入 `util.spawns` require
   - Defense spawn 改用 `getAvailableSpawn()` 取代 `spawns[0]`

## 預期
- core cleaner 便宜很多（energyAvailable 直接決定 body size）
- W48N25 core: 1 隻 cleaner 飛過去打，~300+ DPS
- 現有的 guard_core 仍會繼續跑去打（舊 code 仍支援）
