# Screeps Journal - 2026-06-13 13:55 UTC

## 本輪重點：以最新 sim 為基礎重建 claw-deepseek

Theodos 要求把 sim 當穩定參考源拉回來修改。

## sim vs 舊 new-code 差異

Sim 新增：
- `manager.population.js` — RCL 1-8 人口政策表 + 5 種 fallback 設定檔
- `planner.roomPlanner.js` — 完整室內佈局規劃（含 createConstructionSite）
- `manager.rcl2ContainerEconomy.js` — 改為使用 population.getPlan() 結構化 spawn
- `manager.stats.js`、`role.rcl2Hauler.js`、`role.support.js` — 小改動
- `role.rcl1Upgrader.js` — 已有明確 idle 原因（idle:controller-container-empty、idle:no-controller-container）

## 在 sim 基礎上只加兩個保守改動

### 1. Upgrader controller emergency fallback
- 只在 `economy.controllerEmergency()` (ticksToDowngrade < 4000) 時允許挖 source
- 平常無 container 時保持 idle（不搶 harvester 站位）
- Controller container 有能量時優先使用

### 2. Spawn fallthrough
- Miner/hauler spawn 失敗後不無條件 return，用 spawnBlocked flag
- 失敗後只降級生成 1 隻 emergency harvester
- 不破壞 priority

## 部署
- Sim → 拉最新 22 modules → 複製為 new-code 基礎
- 只在 claw-deepseek 上部署（22 modules: 20 舊 + 2 新）
- sim 完全沒碰
